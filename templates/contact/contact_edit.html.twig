{% extends 'base.html.twig' %}

{% block title %}Contact{% endblock %}

{% block body %}
<section class="container mt-5">
    <div class="text-center mb-4">
        <h1 class="text-white">Edit Contact 📩</h1>
        <p class="text-secondary">Edit your question</p>
    </div>
    <div class="card bg-dark text-white p-4 border-0">
        {{ form_start(form, {'attr': {'class': 'form'}}) }}
        <div class="row g-3">
            <div class="col-12">
                {{ form_row(form.question, {'attr': {'class': 'form-control bg-secondary text-white', 'placeholder': 'Your Question'}}) }}
            </div>
        </div>
        <div class="text-center mt-3">
            <button type="submit" class="btn btn-primary">Edit</button>
        </div>
        {{ form_end(form) }}
    </div>
</section>
{% endblock %}
